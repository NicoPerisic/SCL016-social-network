/* Logout
  
  const outHome = document.getElementById("logout");
  const logout = (e) => {
    e.preventDefault();
  
    auth.signOut().then(() => {
      console.log('chauuuu')
    })
  }
  
  logout.addEventListener('click', logout)
  
  */